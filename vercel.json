{
  "buildCommand": "pnpm build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "env": {
    "APP_ENV": "staging"
  },
  "build": {
    "env": {
      "APP_ENV": "staging"
    }
  }
}
