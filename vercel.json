{
  "buildCommand": "pnpm build:web",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "env": {
    "APP_ENV": "staging"
  },
  "build": {
    "env": {
      "APP_ENV": "staging"
    }
  }
}
